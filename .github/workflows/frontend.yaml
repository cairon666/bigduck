name: Frontend
run-name: ${{ github.actor }} is frontend
on: [push]

jobs:
  Frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 16.13.x
      - name: Install dependencies
        uses: |
          yarn
      - name: Start audit code
        run: make audit