apiVersion: 1

datasources:
- name: Prometheus
  type: prometheus
  url: http://prometheus:9090
  isDefault: true
  access: proxy
  editable: true
#- name: Elastic
#  type: elasticsearch
#  access: proxy
#  url: http://elasticsearch:9200
#  jsonData:
#    index: '[metrics-]YYYY.MM.DD'
#    interval: Daily
#    timeField: '@timestamp'
